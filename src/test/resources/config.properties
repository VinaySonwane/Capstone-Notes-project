ui.base.url=https://practice.expandtesting.com/notes/app
api.base.url=https://practice.expandtesting.com/notes/api
grid.url=http://localhost:4444/wd/hub
test.email=Sonwanevinay11@gmail.com
test.password=@Vinay
# ─── MCP (Model Context Protocol) — Section 3.4 — Grok (xAI) ────────────────
# Set GROK_API_KEY as an environment variable (preferred for CI),
# or paste your key here for local development.
# Grok API docs: https://docs.x.ai/api
mcp.api.url    = https://api.x.ai/v1/chat/completions
# mcp.api.key is intentionally absent here.
# It is loaded from environment.properties (local) or the GROQ_API_KEY
# Jenkins credential (CI).  See ConfigReader for the resolution order.
mcp.model      = grok-3-mini
mcp.max.tokens = 512
